<div class="container mt-5">
  <h1>Edit Task</h1>

  <div class="card">
    <div class="card-body">
      <%= render 'form', task: @task %>

      <%= link_to 'Back to all tasks', tasks_path, class: "btn btn-secondary mt-3" %>
    </div>
  </div>
</div>
